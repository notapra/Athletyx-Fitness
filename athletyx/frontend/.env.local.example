# OpenAI — required for LangChain agent (server-side only, never expose to browser)
OPENAI_API_KEY=sk-your-key-here

# Optional: legacy Python FastAPI router (used when LangChain route fails)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
